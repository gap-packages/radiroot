<!-- W  install.tex          Radiroot documentation           Andreas Distler / Y  2005 -->
<Chapter Label="Installation">
<Heading>Installation</Heading>
<Section Label="Getting and Installing this Package">
<Heading>Getting and Installing this Package</Heading>
This package is available at
<Log><![CDATA[
https://gap-packages.github.io/radiroot/
]]></Log>
in form of a gzipped tar-archive. For installation instructions see
Chapter&nbsp;<Ref Sect="Installing a GAP Package" BookName="ref"/> in the &GAP; reference manual. 
Normally you will unpack the archive in the <C>pkg</C> directory of your
&GAP; version by typing:
<Log><![CDATA[
    bash> tar xfz radiroot-2.9.tar.gz        # for the gzipped tar-archive
]]></Log>
</Section>
<Section Label="Loading and Testing the Package">
<Heading>Loading and Testing the Package</Heading>
To use the &Radiroot; package you have to request it explicitly. This  is
done by calling
<Log><![CDATA[
gap> LoadPackage("radiroot");
-----------------------------------------------------------------------------
Loading  RadiRoot 2.9 (Roots of a Polynomial as Radicals)
by Andreas Distler (a.distler@tu-bs.de).
Homepage: https://gap-packages.github.io/radiroot/
-----------------------------------------------------------------------------
true
]]></Log>
The <C>LoadPackage</C> command is described  in  Section&nbsp;<Ref Func="LoadPackage" BookName="ref"/>  in
the &GAP; reference manual.
<P/>
If you want to load the &Radiroot; package by default, you  can  put  the
<C>LoadPackage</C> command  into  your  <C>gaprc</C>  file  (see  Section&nbsp;<Ref Subsect="The gaprc file" BookName="ref"/> in the &GAP; reference manual).
<P/>
Once the package is loaded, it is possible to check the correct
installation by running the test suite of the package with the command
<Log><![CDATA[
gap> ReadPackage( "radiroot", "tst/testall.g" );
]]></Log>
</Section>
<Section Label="Additional Requirements">
<Heading>Additional Requirements</Heading>
To use &Radiroot; the package &Alnuth; in version 3.0 or higher
has to be loaded with its interface fully functional.
<P/>
In the standard mode a dvi file is created to display the roots of a
polynomial. As default the package uses the command <C>latex</C> searched
for in your system programs to create the dvi file and the command
<C>xdvi</C> to start the dvi viewer. If you can not use this settings you
will have to change the function <C>RR_Display</C> in the file <F>Strings.gi</F>
in the subdirectory <C>lib</C> of the package.
</Section>
</Chapter>
